# ship-statuses

Fixture with a **different** declared status set (`shipped` / `returned`) than `pin-statuses`. Cross-reporting `logged_in` here (or `shipped` on pin-statuses) is rejected by the master.
